between the claude.md and the comments in scripts, there should be enough context that in claude code you should just be able to /plan "i'm missing the 7 referenced patch files, please recreate them using update-kernel-ref to obtain a fresh copy of a cachyos-patched linux source, this Patch.zip I downloaded from the mkopec branch, and use audit-frl-patches.py to check your work" and it should be able to regenerate the fixup patches from scratch." You still need to download the Patch.zip artifact yourself from https://github.com/mkopec/linux/actions (latest "Generate patch" run → "Patch" artifact). I am not distributing mkopec's code — only the tooling to integrate it.

⚠️ IMPORTANT — PLEASE READ ⚠️

 - This is provided as-is, with absolutely no warranty. I assume no responsibility for bricked systems, failed boots, hardware issues, or anything else that happens as a result of running this. You are building and installing a custom kernel — if that sentence makes you nervous, this probably isn't for you yet.
 - Read the code before you run it. These are shell scripts that run makepkg and patch — there's nothing hidden, but you should understand what you're executing on your system. Most of this was vibe coded with Claude Code.
 - This is configured for my hardware — a 7800X3D + 9070 XT. The build uses _processor_opt=zen4 (CONFIG_MZEN4) and the default CachyOS EEVDF+BORE scheduler with Clang ThinLTO. If you're on a different CPU architecture (zen3, zen5, Intel, etc.), you'll need to edit build-cachyos-frl to match your setup.
 - The official CachyOS script-znver4.sh also enables `_build_zfs`, `_build_nvidia_open`, and `_build_r8125`. Our build intentionally skips these — they aren't needed for the FRL use case. If you need them, pass `PKGBUILD_OVERRIDES="_build_zfs=yes _build_nvidia_open=yes _build_r8125=yes" build-cachyos-frl ...`
 - I am not a kernel developer. I'm a web dev who wanted 4K/120Hz HDR over HDMI 2.1 and refused to take no for an answer. This build system was put together with a lot of help from Claude Code. It works on my machine. Your mileage may vary.
 - This is not affiliated with CachyOS or mkopec. Don't bother them with issues caused by this tooling. I'm also not offering support — you are completely on your own. If you suspect problems with Cachy kernel, rollback to a clean cachy kernel. if you suspect problems with the mkopec patchset, the kernel in the AUR is probably a better place to start.
 - If any of this ends up in a legal grey area, you're on your own. I'm sharing build tooling and documentation, not proprietary code. By using this, you accept all risk and liability.
 - cachy owned by cachy; mkopec owned by mkopec; I am unaffiliated with both, just a random loser on the internet who wants to utilize the hardware features he paid for.
